# WinCalendar 验证记录

验证日期：2026-09-07。当前版本为待完成交互验收的试用包。

## 已通过

- 工程在 `D:\workplace\WinCalendar` 构建成功，0 警告、0 错误。
- 40 项基础检查：四语言资源完整性、语言回退、日本区域格式、放假／补班、普通周末、未知年份、休班字段完整性、六周网格、传统节日和闰月、链接验证、配置持久化与损坏保护、全天排他结束日期、重复与例外、跨时区、跨月边界、长事件、取消事件、单次重复改期、折行和非法 ICS。
- 独立发布包执行的 8 项集成检查：子进程及内存限制、离线缓存、网络失败保留、公开 HTTPS 订阅、刷新去重、非法更新保留、非法 ICS 退出、极端重复规则受限。
- 独立包内置 Microsoft.NETCore.App 与 Microsoft.WindowsDesktop.App 8.0.30；官方下载的运行时包 SHA-512 校验通过。
- 简中、繁中、英文、日文分别生成浅色／深色布局图；长节日名截断并通过提示显示全文，未发现布局重叠。
- Computer Use 读取过实际日历窗口和可访问树，确认休班角标和节假日名称已显示。
- 当前 25H2 的 UI Automation 诊断识别到独立时钟按钮 `SystemTray.OmniButtonLeft`。时间区域与相邻通知铃铛边界不重叠，未使用硬编码屏幕坐标。

## 尚待实机确认，不计入通过

- 点击原系统时间直接打开，且没有原生面板闪现。
- 再次点击系统时间、点击外部、Esc 的收起效果。
- 通知铃铛与 Win+N 正常，退出程序恢复原生行为。
- Explorer 重启后重新识别时间入口。
- 多显示器、不同 DPI、任务栏自动隐藏场景。
- 实际切换 Windows 显示语言／区域设置后的即时同步与注销后同步。

已启动后台测试程序并请求交互反馈；记录生成时尚未收到结果。因此不能声称全部硬性验收已经通过。

## 复现

```powershell
dotnet run --project Checks/Checks.csproj
```

独立包集成检查（报告路径使用可写目录）：

```powershell
./WinCalendar.exe --integration-check integration-results.txt
```

集成检查使用临时隔离目录与公开 ICS，不改写用户订阅。真实输出见 `integration-results.txt`。
