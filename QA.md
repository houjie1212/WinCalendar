# WinCalendar 验证记录

## 中国农历开关

- 本次基础检查共 61 项通过，包含农历默认值、设置重载、编辑副本隔离、年界、闰月、支持范围和四语言。
- 独立发布成功；已生成四语言深浅主题的农历开／关预览，检查简中浅色与英文深色农历布局无重叠。
- 实际设置页保存／取消交互待用户确认；节日名称优先、农历加入提示的逻辑已检查。

## 按需节假日订阅改造

本次构建通过，0 警告、0 错误；50 项基础检查、9 项独立发布包集成检查通过。真实集成结果见 `integration-results.txt`。已生成四语言深浅主题预览，并检查简中浅色和英文深色默认公历布局。

- 基础检查覆盖四语言资源、旧配置默认类型、类型持久化、完整标题识别、未知格式、源日期、定时补班、跨年、全天排他结束、多源冲突、启停删除及普通 ICS 回归。
- 集成检查使用隔离目录，覆盖真实子进程、公开 ShuYZ ICS 下载与休班标识、刷新去重、离线缓存、非法更新保留及解析资源限制；不修改用户订阅。
- 旧版内置节假日及农历检查已移除，不再适用于当前版本。

## 实机交互待确认

- 新版默认公历界面与订阅编辑类型选择，添加订阅后名称和休班显示。
- 时钟点击无闪现、再次点击／外部点击／Esc 关闭，通知铃铛与 Win+N、Explorer 重启、多屏 DPI 与自动隐藏任务栏。
- 实际切换 Windows 显示语言／区域格式后的同步。

## 复现

```powershell
dotnet run --project Checks/Checks.csproj
./WinCalendar.exe --integration-check integration-results.txt
```
